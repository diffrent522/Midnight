<ui:FluentWindow x:Class="Midnight.Views.LaunchOptionsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Title="Launch Options" Height="350" Width="450"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ExtendsContentIntoTitleBar="True"
        WindowBackdropType="Mica"
        WindowCornerPreference="Round">
    
    <Grid>
        <Grid.RowDefinitions>
             <RowDefinition Height="Auto"/>
             <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <ui:TitleBar Title="Launch Options" Grid.Row="0"/>

        <ui:Card Grid.Row="1" Margin="20" Padding="20" VerticalAlignment="Stretch">
            <StackPanel>
                <TextBlock Text="Game Join Configuration" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,20"/>

                <!-- Place ID -->
                <TextBlock Text="Place ID (Required for specific server)" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Margin="0,0,0,5"/>
                <ui:TextBox x:Name="TxtPlaceId" PlaceholderText="123456789" Margin="0,0,0,15"/>

                <!-- Job ID / Link -->
                <TextBlock Text="Job ID or Private Server Link (Optional)" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Margin="0,0,0,5"/>
                <Grid Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <ui:TextBox x:Name="TxtJobId" PlaceholderText="UUID or Link..."/>
                    <ui:Button Grid.Column="1" Content="Browser" Width="80" Click="BtnBrowser_Click" Margin="10,0,0,0" ToolTip="Browse Public Servers" Appearance="Secondary" Icon="{ui:SymbolIcon GlobeSearch24}"/>
                </Grid>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                    <ui:Button Content="Cancel" Width="80" Click="BtnCancel_Click" Appearance="Secondary" Margin="0,0,10,0"/>
                    <ui:Button Content="Launch" Width="100" Click="BtnLaunch_Click" Appearance="Primary" Icon="{ui:SymbolIcon Rocket24}"/>
                </StackPanel>
            </StackPanel>
        </ui:Card>
    </Grid>
</ui:FluentWindow>

