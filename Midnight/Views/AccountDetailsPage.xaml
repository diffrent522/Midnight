<Page x:Class="Midnight.Views.AccountDetailsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="800">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Header -->
            <RowDefinition Height="Auto"/> <!-- Content -->
            <RowDefinition Height="*"/>    <!-- Spacer -->
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
            <ui:Button Icon="{ui:SymbolIcon ArrowLeft24}" Click="BtnBack_Click" Appearance="Secondary" Margin="0,0,15,0" Foreground="White"/>
            <TextBlock Text="Edit Account" FontSize="24" FontWeight="SemiBold" VerticalAlignment="Center" Foreground="White"/>
        </StackPanel>

        <ui:Card Grid.Row="1" VerticalAlignment="Stretch">
            <Grid Margin="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Avatar Column -->
                <StackPanel Grid.Column="0" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,10,20,0">
                    <Ellipse Width="120" Height="120" Margin="0,0,0,15">
                        <Ellipse.Fill>
                            <ImageBrush ImageSource="{Binding AvatarUrl}"/>
                        </Ellipse.Fill>
                    </Ellipse>
                    <TextBlock Text="{Binding Username}" HorizontalAlignment="Center" FontWeight="Bold" FontSize="16"/>
                    <TextBlock Text="{Binding UserId}" HorizontalAlignment="Center" Foreground="{DynamicResource TextFillColorSecondaryBrush}" FontSize="12"/>
                    <TextBlock Text="{Binding ExpirationText, StringFormat='Expires: {0}'}" HorizontalAlignment="Center" Margin="0,5,0,0" Foreground="{DynamicResource PaletteRedBrush}" FontSize="11" TextWrapping="Wrap" TextAlignment="Center"/>
                </StackPanel>

                <!-- Form Column -->
                <StackPanel Grid.Column="1" VerticalAlignment="Top">
                     <TextBlock Text="Local Settings" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,15"/>
                     
                     <TextBlock Text="Alias (Nickname)" Margin="0,0,0,5"/>
                     <ui:TextBox Text="{Binding AliasEditBuffer, UpdateSourceTrigger=PropertyChanged}" PlaceholderText="Enter a custom name..." Margin="0,0,0,20"/>

                     <TextBlock Text="Group" Margin="0,0,0,5"/>
                     <ui:TextBox Text="{Binding GroupEditBuffer, UpdateSourceTrigger=PropertyChanged}" PlaceholderText="Main, Storage, etc." Margin="0,0,0,20"/>

                     <TextBlock Text="HTTP Proxy" Margin="0,0,0,5"/>
                     <ui:TextBox Text="{Binding ProxyEditBuffer, UpdateSourceTrigger=PropertyChanged}" PlaceholderText="http://user:pass@host:port" Margin="0,0,0,20"/>
                     
                     <TextBlock Text="Description / Notes" Margin="0,0,0,5"/>
                     <ui:TextBox Text="{Binding DescriptionEditBuffer, UpdateSourceTrigger=PropertyChanged}" PlaceholderText="Add notes about this account..." Margin="0,0,0,20" Height="100" AcceptsReturn="True" TextWrapping="Wrap" VerticalContentAlignment="Top"/>

                     <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                         <ui:Button Content="Cancel" Command="{Binding CancelAndBackCommand}" Appearance="Secondary" Margin="0,0,10,0" Width="100"/>
                         <ui:Button Content="Save Changes" Command="{Binding SaveAndBackCommand}" Appearance="Primary" Width="120"/>
                     </StackPanel>
                </StackPanel>
            </Grid>
        </ui:Card>
    </Grid>
</Page>

