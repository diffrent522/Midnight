<Window x:Class="Midnight.Views.BrowserWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        mc:Ignorable="d"
        Title="Roblox Browser" Height="800" Width="1200"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource Brush.Background.Dark}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Title Bar / Toolbar -->
        <Grid Background="{DynamicResource Brush.Surface.Dark}" Height="40">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock x:Name="TitleText" Text="Roblox Browser" VerticalAlignment="Center" Margin="15,0" FontWeight="SemiBold" Foreground="{DynamicResource TextFillColorPrimaryBrush}"/>
            
            <StackPanel Grid.Column="2" Orientation="Horizontal" Margin="0,0,10,0">
                <ui:Button Icon="{ui:SymbolIcon ArrowClockwise24}" Click="Reload_Click" Appearance="Transparent" ToolTip="Reload"/>
            </StackPanel>
        </Grid>

        <!-- Browser -->
        <wv2:WebView2 Grid.Row="1" x:Name="Browser"/>
    </Grid>
</Window>

