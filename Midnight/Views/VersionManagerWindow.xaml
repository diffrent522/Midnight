<Window x:Class="Midnight.Views.VersionManagerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:Midnight.ViewModels"
        mc:Ignorable="d"
        Title="Version Manager" Height="450" Width="600"
        Background="{StaticResource Brush.Background.Dark}"
        WindowStartupLocation="CenterOwner">

    <Window.DataContext>
        <vm:VersionManagerViewModel/>
    </Window.DataContext>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Version Manager" FontSize="20" FontWeight="Bold" Foreground="{StaticResource Brush.Text.Primary}" Margin="0,0,0,20"/>

        <!-- Version List -->
        <Border Grid.Row="1" Background="{StaticResource Brush.Surface.Dark}" CornerRadius="8" Padding="15">
            <StackPanel Grid.IsSharedSizeScope="True">
                
                <!-- Installed -->
                <Grid Margin="0,0,0,15">
                     <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" SharedSizeGroup="Label"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Installed Version:" Foreground="{StaticResource Brush.Text.Secondary}" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <TextBlock Grid.Column="1" Text="{Binding InstalledVersion}" Foreground="{StaticResource Brush.Accent}" FontWeight="Bold" VerticalAlignment="Center" FontSize="16"/>
                    <!-- Button removed -->
                </Grid>

                <Rectangle Height="1" Fill="#333" Margin="0,0,0,15"/>

                <!-- Current (Live) -->
                <Grid Margin="0,0,0,10">
                     <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" SharedSizeGroup="Label"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Latest (Live):" Foreground="#28a745" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <TextBlock Grid.Column="1" Text="{Binding CurrentVersion}" Foreground="White" VerticalAlignment="Center"/>
                    <Button Grid.Column="2" Content="Download" Command="{Binding OpenDownloadCommand}" CommandParameter="Current" Width="75"/>
                </Grid>

                <!-- Future -->
                <Grid Margin="0,0,0,10">
                     <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" SharedSizeGroup="Label"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Future (Next):" Foreground="#17a2b8" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <TextBlock Grid.Column="1" Text="{Binding FutureVersion}" Foreground="White" VerticalAlignment="Center"/>
                    <Button Grid.Column="2" Content="Download" Command="{Binding OpenDownloadCommand}" CommandParameter="Future" Width="75"/>
                </Grid>

                <!-- Past -->
                <Grid>
                     <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" SharedSizeGroup="Label"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Past (Downgrade):" Foreground="#d63384" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <TextBlock Grid.Column="1" Text="{Binding PastVersion}" Foreground="White" VerticalAlignment="Center"/>
                    <Button Grid.Column="2" Content="Download" Command="{Binding OpenDownloadCommand}" CommandParameter="Past" Width="75"/>
                </Grid>
                
                <!-- Custom Path moved to Settings -->
                
            </StackPanel>
        </Border>

        <!-- Status & Actions -->
        <Grid Grid.Row="2" Margin="0,15,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <TextBlock Text="{Binding StatusMessage}" Foreground="{StaticResource Brush.Text.Secondary}" VerticalAlignment="Center"/>
            
            <Button Grid.Column="1" Content="Refresh" Command="{Binding RefreshCommand}" Width="100"/>
        </Grid>
    </Grid>
</Window>

