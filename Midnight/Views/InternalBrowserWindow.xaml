<Window x:Class="Midnight.Views.InternalBrowserWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        mc:Ignorable="d"
        Title="Download Version - RDD" Height="600" Width="900"
        Background="{StaticResource Brush.Background.Dark}"
        WindowStartupLocation="CenterOwner">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Padding="10" Background="{StaticResource Brush.Surface.Dark}">
            <TextBlock Text="RDD Downloader (Internal)" Foreground="White" FontWeight="Bold"/>
        </Border>

        <wv2:WebView2 Grid.Row="1" Name="Browser" Source="about:blank"/>
    </Grid>
</Window>

