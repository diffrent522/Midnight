<Page x:Class="Midnight.Views.BrowserPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:vm="clr-namespace:Midnight.ViewModels"
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="800"
      Title="Browser Launcher">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
            <ui:Button Icon="{ui:SymbolIcon ArrowLeft24}" Command="{Binding NavigateBackCommand}" Appearance="Secondary" Margin="0,0,15,0"/>
            <TextBlock Text="Roblox Browser" FontSize="20" FontWeight="Bold" VerticalAlignment="Center" Foreground="{DynamicResource TextFillColorPrimaryBrush}"/>
        </StackPanel>
        
        <!-- Account List -->
        <ui:Card Grid.Row="1" VerticalAlignment="Top">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                
                <TextBlock Text="Select Accounts to Open" FontWeight="SemiBold" FontSize="16" Margin="0,0,0,10"/>
                
                <ListView Grid.Row="1" ItemsSource="{Binding AccountItems}" SelectionMode="Single" Background="Transparent" BorderThickness="0">
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                             <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                             <Setter Property="Focusable" Value="False"/>
                             <Setter Property="IsHitTestVisible" Value="True"/>
                        </Style>
                    </ListView.ItemContainerStyle>
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="0,5">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/> <!-- Avatar -->
                                    <ColumnDefinition Width="*"/>    <!-- Name -->
                                    <ColumnDefinition Width="Auto"/> <!-- Checkbox -->
                                </Grid.ColumnDefinitions>

                                <!-- Avatar -->
                                <Border Width="32" Height="32" CornerRadius="16" Margin="0,0,10,0" Background="#33FFFFFF">
                                    <Border.Effect>
                                        <DropShadowEffect ShadowDepth="1" BlurRadius="5" Opacity="0.2"/>
                                    </Border.Effect>
                                    <Image Source="{Binding Account.AvatarUrl, TargetNullValue=http://www.roblox.com/images/default_avatar.jpg}" Stretch="UniformToFill">
                                        <Image.Clip>
                                            <EllipseGeometry Center="16,16" RadiusX="16" RadiusY="16"/>
                                        </Image.Clip>
                                    </Image>
                                </Border>

                                <!-- Name -->
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding Account.DisplayName}" FontWeight="SemiBold"/>
                                    <TextBlock Text="{Binding Account.Username}" FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                                </StackPanel>

                                <!-- Checkbox -->
                                <CheckBox Grid.Column="2" IsChecked="{Binding IsSelected, Mode=TwoWay}" VerticalAlignment="Center"/>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </ui:Card>

        <!-- Action Bar -->
        <Grid Grid.Row="2" Margin="0,20,0,0">
             <ui:Button Appearance="Primary" Icon="{ui:SymbolIcon Globe24}" Content="Open Selected Browser(s)" Command="{Binding OpenSelectedCommand}" HorizontalAlignment="Right" Width="220" Height="40"/>
        </Grid>
    </Grid>
</Page>

