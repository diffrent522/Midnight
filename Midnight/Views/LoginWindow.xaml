<ui:FluentWindow x:Class="Midnight.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Title="Roblox Login" Height="600" Width="400"
        WindowStartupLocation="CenterScreen"
        ExtendsContentIntoTitleBar="True"
        WindowBackdropType="Mica"
        WindowCornerPreference="Round">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ui:TitleBar Title="Roblox Login" Grid.Row="0"/>

        <ui:Card Grid.Row="1" Margin="10" Padding="10">
            <TextBlock Text="Log in to your Roblox account. we will automatically capture the session." 
                       TextWrapping="Wrap" TextAlignment="Center" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
        </ui:Card>

        <wv2:WebView2 Grid.Row="2" x:Name="LoginWebView" Source="https://www.roblox.com/login" Margin="10,0"/>

        <Grid Grid.Row="3" Margin="10">
            <ui:Button Content="Cancel" Click="BtnCancel_Click" Appearance="Secondary" HorizontalAlignment="Center" Width="100"/>
        </Grid>
    </Grid>
</ui:FluentWindow>

